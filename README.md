# beep-cert-creatives — ad-image CDN + creative scoreboard
PUBLIC repo: raw URLs are used directly as Meta ad images (Build SOP). registry.csv = the scoreboard (R2 appends, R3 judges/promotes, R1 kills). winners/ = the Higgsfield image-to-image reference pool (R3-curated only).
