# winners/ — the reference pool R2 anchors Higgsfield on
Export these 6 proven creatives from Ads Manager (Account -> Images, match the image hashes in registry.csv) and commit the PNGs here with the filenames used in intelligence/angle-weights.json:
hr_wachat.png · e03_trust.png · east_ai_dark.png · dm_chatgpt.png · interviewproof_da.png · sharktank_hero.png
Until the PNGs land, R2 falls back to fetching each hash's permalink via the Meta API (adimages endpoint) at run time. New winners are added ONLY by R3 (banked >=1.6, >=Rs500, >=10 purchases).
